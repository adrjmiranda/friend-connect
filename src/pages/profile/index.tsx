import { FunctionComponent, ReactNode } from 'react';

import * as Style from './style';

const Profile: FunctionComponent = (): ReactNode => {
	return (
		<Style.Container>
			<h1>Profile</h1>
		</Style.Container>
	);
};

export default Profile;
