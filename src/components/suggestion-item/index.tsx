import { FunctionComponent, ReactNode } from 'react';

import * as Style from './style';

const SuggestionItem: FunctionComponent = (): ReactNode => {
	return (
		<Style.Container>
			<h1>SuggestionItem</h1>
		</Style.Container>
	);
};

export default SuggestionItem;
