import styled from 'styled-components';

export const Container = styled.div`
	flex: 6;
	display: flex;
	flex-direction: column;
	gap: 2rem;

	.posts {
		display: flex;
		flex-direction: column;
		gap: 4rem;
	}
`;
