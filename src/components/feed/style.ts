import styled from 'styled-components';

export const Container = styled.div`
	flex: 5;
	display: flex;
	flex-direction: column;
	gap: 2rem;

	.posts {
		display: flex;
		flex-direction: column;
		gap: 4rem;
	}
`;
